<?xml version="1.0" encoding="utf-8" ?>

<opc:ModelDesign
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:xsd="http://www.w3.org/2001/XMLSchema"
  xmlns:opc="http://opcfoundation.org/UA/ModelDesign.xsd"
  xmlns:ua="http://opcfoundation.org/UA/"
  xmlns:uax="http://opcfoundation.org/UA/2008/02/Types.xsd"
  xmlns="{{ ns_urn }}"
  TargetNamespace="{{ ns_urn }}"
>

  <opc:Namespaces>
    <opc:Namespace Prefix="{{ ns_urn.nss().replace(':', ".") }}">{{ ns_urn }}</opc:Namespace>
  </opc:Namespaces>

{%- for object_type in object_types %}

  <opc:ObjectType SymbolicName="{{ object_type.name }}" BaseType="ua:BaseObjectType">
    <opc:Description>{{ object_type.description }}</opc:Description>
    <opc:Children>
{%- for var in object_type.variable %}
      <opc:Property SymbolicName="{{ var.name }}" DataType="ua:{{ var.data_type }}" {# Keep the trailing space #}
        {%- if let Some(dimensions) = var.array_dimensions -%}
          ValueRank="Array" ArrayDimensions="{{ dimensions | join(',') }}"
        {%- endif -%}
      >
        <opc:Description>{{ var.description }}</opc:Description>
      </opc:Property>
{%- endfor %}
    </opc:Children>
  </opc:ObjectType>
{%- endfor %}

</opc:ModelDesign>

